Вітаю та дякую, що перевіряєте дану роботу та читаєте це 
Вмикається усе як на занняттях, а саме 
1.інсталяція всіх бібліотек
2.start:docker:db в packege.json 
3.start:local в packege.json 

База даних Postgress щоб легко додатись до вже існуючої

Nest з системою модулів які легко можна забрати і додати щоб проект був адаптивний

Логіка ролей була задумана колонкою role 
в baseModelUser 
@ApiProperty({ enum: ['User', 'Admin', 'Moderator']})
  type: string;
і викориcтанням їх з iUserData
Так само з типом акаунта через type
 @ApiProperty({ enum: ['base', 'premium', 'banned']})
  type: string;
яку повинен змінювати адмін

логіка додання AWS - додана як загрузка фотографій в оголошення в проекті тільки 1 фото 
було багато спроб зробити таблицею в мене не вийшло

перевірка на нецензурну лексику була задумана як декоратор
@UsePipes(new ValidationPipe({ transform: true }))
над методом create в сar.controler, його логіка в папці validator

логіка переглядів була в модула views і реалізовувалася при кліку на getCar 
робилась таблиця з carId та userId і baseModule і вже з неї по даті діставати дані для преміум акаунта 
але видалена - неробоча і взагалі соромно показувати

для діставання по регіонах була колонка locate і вже з неї через QueryBilder була задумка діставати і селектити по запиту 

так само з ціною і колонка price
і викориcтанням їх з iСarData


задумка валют була в діставанні з апі приватБанку відповідних полів і колонки valut в car
https://api.privatbank.ua/p24api/pubinfo?exchange&coursid=5

Загалом протягом 2х тижнів з тими виключаннями остаточно згорів пк і все дописувалось в бібліотеці політехніки
Однак це не виправдання 
Хоч я і був присутній та працював на кожній лекції та передивлявся кожне відео по декілька раз мені дуже соромно за код який ви читаєте 
я можу набагато краще і буду далі розвиватись





